@model IEnumerable<CakeModel>


<div class="py-5">

    <div class="container">

         <div class="row py-5 ">
                <div class="col-lg-5 m-auto text-center">
                    <h1>
                        Celebrate In Style </h1>
                    <h6 style="color: red;">   Birthday Cakes </h6>
                    
                </div>
            </div>
       
            </div>

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-0 ">
            @if (Model != null)
            {
                @if (Model.Count() > 0)
                {
                    @foreach (var Cake in Model)
                    {

                        <div class="card-body">
                            <div class="card">
                            <a type="submit" class="btn2" asp-controller="Cake" asp-action="Detalhes" asp-route-id="@Cake.Id">

                                <img src="/imagem/@Cake.Capa" class="img-inside-card"  alt="">
                            </a>

                            </div>
                            <p>
                            <h6 class="text-body-card">@Cake.Sabor</h6>
                            </p>


                        </div>
                    }
                }
            }
        </div>

    </div>
</div>